*wallet.dat
wallet-next/pwalletd-db.sqlite
wallet-next/pwalletd-db.sqlite-shm
wallet-next/pwalletd-db.sqlite-wal

*.DS_STORE

# mdbook docs builds
docs/*/book/*
# firebase config
.firebase/
firebase-debug.log
docs/*/firebase-tmp/
# ignore auto-generated rust-docs index
docs/rustdoc/index.html

# Nix build output goes here
result

# Direnv files
.envrc
.direnv

## Rust / Cargo
# will have compiled files and executables
debug/
target/

# We can't trust the ecosystem to use semver properly, so we have to check in a lockfile.
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# editor
.swp
.idea
.vscode

# Python3
.venv

# Ignoring lock file for the parameter-setup tool.
tools/parameter-setup/Cargo.lock

# Relayer config referencing local devnet, will always be unique
# to current host env.
deployments/relayer/configs/penumbra-local.json

# Logs, and other files from smoke tests
deployments/logs/
deployments/worktrees/
deployments/bin/
crates/bin/pcli/proposal.toml
phase1.bin
crates/test/migration-test/proposal.toml

# Memory profiler, via bytehound or otherwise
*.dat

# npm dependencies
node_modules
build
