{
    /*
    // Setting this variable will cause rust-analyzer to perform
    // compile-time checks against the sqlite instance in this location.
    // Disabling it will cause rust-analyzer to use the cached
    // data in sqlx-data.json.
    // We disable it by default, to avoid errors when not working with the view service.
    "rust-analyzer.server.extraEnv": {
        "DATABASE_URL": "sqlite:///tmp/pviewd-dev-db.sqlite"
    },
    */
}
