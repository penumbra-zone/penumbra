apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-cluster.fullname" . }}
  labels:
    {{- include "node-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-cluster.selectorLabels" . | nindent 4 }}
