apiVersion: v1
kind: ConfigMap
metadata:
  name: tm-config
data:
  config.toml: |
    [p2p]
    seeds = ""

    persistent_peers = "{{ $.Files.Get "persistent_peers.txt" | trim }}"

    max_num_inbound_peers = 50
    max_num_outbound_peers = 50

    [tx_index]
    indexer = "null"

    [instrumentation]
    prometheus = true
